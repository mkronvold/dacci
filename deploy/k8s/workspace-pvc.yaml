apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # Compatibility name retained for now; this PVC stores the content-repo checkout.
  name: dacci-workspace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
